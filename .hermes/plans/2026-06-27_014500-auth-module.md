# Module Auth — Plan d'implémentation

> **For Hermes:** Use test-driven-development skill for each task.

**Goal:** Authentification JWT complète — register, login, guards par rôle (CUSTOMER / ADMIN / DELIVERY).

**Architecture:** NestJS module Auth avec JWT stateless. `AuthService` gère register/login, `JwtStrategy` valide les tokens, `RolesGuard` restreint les endpoints. DTOs validés avec `class-validator`. Mots de passe hashés avec `bcrypt`.

**Tech Stack:** NestJS 11, Prisma 7, PostgreSQL, @nestjs/jwt, @nestjs/passport, passport-jwt, bcrypt, class-validator, Jest 30

---

## Pré-requis

### Task 0: Installer les dépendances

**Objective:** Ajouter les packages nécessaires à l'auth.

**Files:**
- Modify: `backend/package.json`

**Step 1: Installer**

```bash
cd backend
npm install @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt class-validator class-transformer
npm install -D @types/passport-jwt @types/bcrypt
```

**Step 2: Vérifier**

```bash
node -e "require('@nestjs/jwt'); require('bcrypt'); console.log('OK')"
```
Expected: `OK`

---

## Task 1: Créer le module Auth (structure)

**Objective:** Créer la structure du module Auth avec PrismaService.

**Files:**
- Create: `backend/src/prisma/prisma.service.ts`
- Create: `backend/src/prisma/prisma.module.ts`
- Create: `backend/src/auth/auth.module.ts`
- Create: `backend/src/auth/auth.controller.ts` (stub)
- Create: `backend/src/auth/auth.service.ts` (stub)
- Modify: `backend/src/app.module.ts`

**Step 1: Créer PrismaService**

```typescript
// backend/src/prisma/prisma.service.ts
import { Injectable, OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

```typescript
// backend/src/prisma/prisma.module.ts
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class PrismaModule {}
```

**Step 2: Créer le stub AuthModule**

```typescript
// backend/src/auth/auth.module.ts
import { Module } from '@nestjs/common';
import { JwtModule } from '@nestjs/jwt';
import { PassportModule } from '@nestjs/passport';
import { AuthService } from './auth.service';
import { AuthController } from './auth.controller';

@Module({
  imports: [
    PassportModule,
    JwtModule.register({
      secret: process.env.JWT_SECRET || 'dev-secret-change-me',
      signOptions: { expiresIn: '7d' },
    }),
  ],
  controllers: [AuthController],
  providers: [AuthService],
  exports: [AuthService, JwtModule],
})
export class AuthModule {}
```

```typescript
// backend/src/auth/auth.controller.ts
import { Controller } from '@nestjs/common';

@Controller('auth')
export class AuthController {}
```

```typescript
// backend/src/auth/auth.service.ts
import { Injectable } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class AuthService {
  constructor(private readonly prisma: PrismaService) {}
}
```

**Step 3: Mettre à jour AppModule**

```typescript
// backend/src/app.module.ts
import { Module } from '@nestjs/common';
import { PrismaModule } from './prisma/prisma.module';
import { AuthModule } from './auth/auth.module';
import { AppController } from './app.controller';
import { AppService } from './app.service';

@Module({
  imports: [PrismaModule, AuthModule],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

**Step 4: Vérifier la compilation**

```bash
cd backend && npx tsc --noEmit
```
Expected: silence (no errors)

---

## Task 2: DTOs — Register et Login

**Objective:** Créer les DTOs validés pour register et login.

**Files:**
- Create: `backend/src/auth/dto/register.dto.ts`
- Create: `backend/src/auth/dto/login.dto.ts`

**Step 1: Écrire les tests qui échouent (validation)**

```typescript
// backend/src/auth/dto/register.dto.spec.ts
import { validate } from 'class-validator';
import { RegisterDto } from './register.dto';

describe('RegisterDto', () => {
  it('should reject empty email', async () => {
    const dto = new RegisterDto();
    dto.email = '';
    dto.password = 'Secret123';
    dto.fullName = 'John Doe';
    dto.phone = '+242060000000';
    const errors = await validate(dto);
    expect(errors.length).toBeGreaterThan(0);
    expect(errors[0].property).toBe('email');
  });

  it('should reject short password', async () => {
    const dto = new RegisterDto();
    dto.email = 'john@test.com';
    dto.password = '123';
    dto.fullName = 'John Doe';
    dto.phone = '+242060000000';
    const errors = await validate(dto);
    const passwordError = errors.find((e) => e.property === 'password');
    expect(passwordError).toBeDefined();
  });

  it('should accept valid dto', async () => {
    const dto = new RegisterDto();
    dto.email = 'john@test.com';
    dto.password = 'Secret123';
    dto.fullName = 'John Doe';
    dto.phone = '+242060000000';
    const errors = await validate(dto);
    expect(errors.length).toBe(0);
  });
});
```

**Step 2: Run tests → FAIL**

```bash
cd backend && npx jest src/auth/dto/register.dto.spec.ts
```
Expected: FAIL — RegisterDto not defined

**Step 3: Implémenter RegisterDto**

```typescript
// backend/src/auth/dto/register.dto.ts
import { IsEmail, IsString, MinLength, MaxLength, Matches } from 'class-validator';

export class RegisterDto {
  @IsEmail({}, { message: 'Email invalide' })
  email!: string;

  @IsString()
  @MinLength(6, { message: 'Mot de passe : 6 caractères minimum' })
  @MaxLength(100)
  password!: string;

  @IsString()
  @MinLength(2, { message: 'Nom complet requis' })
  fullName!: string;

  @IsString()
  @Matches(/^\+?[0-9]{10,15}$/, { message: 'Numéro de téléphone invalide' })
  phone!: string;
}
```

**Step 4: Implémenter LoginDto**

```typescript
// backend/src/auth/dto/login.dto.ts
import { IsEmail, IsString } from 'class-validator';

export class LoginDto {
  @IsEmail({}, { message: 'Email invalide' })
  email!: string;

  @IsString()
  password!: string;
}
```

**Step 5: Run tests → PASS**

```bash
cd backend && npx jest src/auth/dto/register.dto.spec.ts
```
Expected: 3 passed

---

## Task 3: AuthService — register() et login()

**Objective:** Implémenter register (hash + create user) et login (validate + JWT).

**Files:**
- Create: `backend/src/auth/auth.service.spec.ts`
- Modify: `backend/src/auth/auth.service.ts`

**Step 1: Écrire les tests (mock PrismaService)**

```typescript
// backend/src/auth/auth.service.spec.ts
import { Test, TestingModule } from '@nestjs/testing';
import { AuthService } from './auth.service';
import { PrismaService } from '../prisma/prisma.service';
import { JwtService } from '@nestjs/jwt';
import { ConflictException, UnauthorizedException } from '@nestjs/common';
import * as bcrypt from 'bcrypt';

describe('AuthService', () => {
  let service: AuthService;
  let prisma: any;
  let jwt: any;

  beforeEach(async () => {
    prisma = { user: { findUnique: jest.fn(), create: jest.fn() } };
    jwt = { sign: jest.fn().mockReturnValue('fake-token') };

    const module: TestingModule = await Test.createTestingModule({
      providers: [
        AuthService,
        { provide: PrismaService, useValue: prisma },
        { provide: JwtService, useValue: jwt },
      ],
    }).compile();

    service = module.get<AuthService>(AuthService);
  });

  describe('register', () => {
    it('should create a user and return token', async () => {
      prisma.user.findUnique.mockResolvedValue(null);
      prisma.user.create.mockResolvedValue({
        id: 'u1', email: 'test@test.com', fullName: 'Test', phone: '+242060000000', role: 'CUSTOMER',
      });

      const result = await service.register({
        email: 'test@test.com', password: 'Secret123', fullName: 'Test', phone: '+242060000000',
      });

      expect(result.access_token).toBe('fake-token');
      expect(result.user.email).toBe('test@test.com');
      expect(prisma.user.create).toHaveBeenCalled();
    });

    it('should reject duplicate email', async () => {
      prisma.user.findUnique.mockResolvedValue({ id: 'existing' });
      await expect(
        service.register({ email: 'test@test.com', password: 'Secret123', fullName: 'Test', phone: '+242060000000' }),
      ).rejects.toThrow(ConflictException);
    });
  });

  describe('login', () => {
    it('should return token for valid credentials', async () => {
      const hash = await bcrypt.hash('Secret123', 10);
      prisma.user.findUnique.mockResolvedValue({ id: 'u1', email: 'test@test.com', passwordHash: hash, fullName: 'T', phone: 'p', role: 'CUSTOMER' });

      const result = await service.login({ email: 'test@test.com', password: 'Secret123' });
      expect(result.access_token).toBe('fake-token');
    });

    it('should reject wrong password', async () => {
      const hash = await bcrypt.hash('Secret123', 10);
      prisma.user.findUnique.mockResolvedValue({ id: 'u1', email: 'test@test.com', passwordHash: hash });

      await expect(
        service.login({ email: 'test@test.com', password: 'WrongPass' }),
      ).rejects.toThrow(UnauthorizedException);
    });

    it('should reject unknown email', async () => {
      prisma.user.findUnique.mockResolvedValue(null);
      await expect(
        service.login({ email: 'x@x.com', password: 'Secret123' }),
      ).rejects.toThrow(UnauthorizedException);
    });
  });
});
```

**Step 2: Run tests → FAIL**

```bash
cd backend && npx jest src/auth/auth.service.spec.ts
```
Expected: FAIL — register/login not implemented

**Step 3: Implémenter AuthService**

```typescript
// backend/src/auth/auth.service.ts
import {
  Injectable,
  ConflictException,
  UnauthorizedException,
} from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import * as bcrypt from 'bcrypt';
import { PrismaService } from '../prisma/prisma.service';
import { RegisterDto } from './dto/register.dto';
import { LoginDto } from './dto/login.dto';

@Injectable()
export class AuthService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly jwtService: JwtService,
  ) {}

  async register(dto: RegisterDto) {
    const existing = await this.prisma.user.findUnique({ where: { email: dto.email } });
    if (existing) {
      throw new ConflictException('Cet email est déjà utilisé');
    }

    const passwordHash = await bcrypt.hash(dto.password, 10);
    const user = await this.prisma.user.create({
      data: {
        email: dto.email,
        passwordHash,
        fullName: dto.fullName,
        phone: dto.phone,
      },
      select: { id: true, email: true, fullName: true, phone: true, role: true },
    });

    const access_token = this.jwtService.sign({ sub: user.id, role: user.role });
    return { access_token, user };
  }

  async login(dto: LoginDto) {
    const user = await this.prisma.user.findUnique({ where: { email: dto.email } });
    if (!user) {
      throw new UnauthorizedException('Email ou mot de passe incorrect');
    }

    const valid = await bcrypt.compare(dto.password, user.passwordHash);
    if (!valid) {
      throw new UnauthorizedException('Email ou mot de passe incorrect');
    }

    const access_token = this.jwtService.sign({ sub: user.id, role: user.role });
    return {
      access_token,
      user: { id: user.id, email: user.email, fullName: user.fullName, phone: user.phone, role: user.role },
    };
  }
}
```

**Step 4: Run tests → PASS**

```bash
cd backend && npx jest src/auth/auth.service.spec.ts
```
Expected: 5 passed

---

## Task 4: AuthController — POST /auth/register et /auth/login

**Objective:** Exposer les endpoints REST.

**Files:**
- Create: `backend/src/auth/auth.controller.spec.ts`
- Modify: `backend/src/auth/auth.controller.ts`

**Step 1: Écrire le test e2e**

```typescript
// backend/src/auth/auth.controller.spec.ts
import { Test, TestingModule } from '@nestjs/testing';
import { AuthController } from './auth.controller';
import { AuthService } from './auth.service';

describe('AuthController', () => {
  let controller: AuthController;
  let authService: any;

  beforeEach(async () => {
    authService = { register: jest.fn(), login: jest.fn() };
    const module: TestingModule = await Test.createTestingModule({
      controllers: [AuthController],
      providers: [{ provide: AuthService, useValue: authService }],
    }).compile();
    controller = module.get<AuthController>(AuthController);
  });

  it('should call authService.register on POST /auth/register', async () => {
    const dto = { email: 't@t.com', password: 'Secret123', fullName: 'T', phone: '+242060000000' };
    authService.register.mockResolvedValue({ access_token: 'tok', user: { id: '1' } });
    const result = await controller.register(dto);
    expect(authService.register).toHaveBeenCalledWith(dto);
    expect(result.access_token).toBe('tok');
  });

  it('should call authService.login on POST /auth/login', async () => {
    const dto = { email: 't@t.com', password: 'Secret123' };
    authService.login.mockResolvedValue({ access_token: 'tok', user: { id: '1' } });
    const result = await controller.login(dto);
    expect(authService.login).toHaveBeenCalledWith(dto);
  });
});
```

**Step 2: Run tests → FAIL**

```bash
cd backend && npx jest src/auth/auth.controller.spec.ts
```
Expected: FAIL — endpoints not defined

**Step 3: Implémenter AuthController**

```typescript
// backend/src/auth/auth.controller.ts
import { Controller, Post, Body, HttpCode, HttpStatus } from '@nestjs/common';
import { AuthService } from './auth.service';
import { RegisterDto } from './dto/register.dto';
import { LoginDto } from './dto/login.dto';

@Controller('auth')
export class AuthController {
  constructor(private readonly authService: AuthService) {}

  @Post('register')
  async register(@Body() dto: RegisterDto) {
    return this.authService.register(dto);
  }

  @Post('login')
  @HttpCode(HttpStatus.OK)
  async login(@Body() dto: LoginDto) {
    return this.authService.login(dto);
  }
}
```

**Step 4: Run tests → PASS**

```bash
cd backend && npx jest src/auth/auth.controller.spec.ts
```
Expected: 2 passed

---

## Task 5: JWT Strategy + Guards

**Objective:** Protéger les routes avec JWT et rôles.

**Files:**
- Create: `backend/src/auth/jwt.strategy.ts`
- Create: `backend/src/auth/roles.guard.ts`
- Create: `backend/src/auth/roles.decorator.ts`
- Create: `backend/src/auth/auth.guard.ts` (JwtAuthGuard)

**Step 1: JwtStrategy**

```typescript
// backend/src/auth/jwt.strategy.ts
import { Injectable, UnauthorizedException } from '@nestjs/common';
import { PassportStrategy } from '@nestjs/passport';
import { ExtractJwt, Strategy } from 'passport-jwt';
import { PrismaService } from '../prisma/prisma.service';

interface JwtPayload {
  sub: string;
  role: string;
}

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(private readonly prisma: PrismaService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      secretOrKey: process.env.JWT_SECRET || 'dev-secret-change-me',
    });
  }

  async validate(payload: JwtPayload) {
    const user = await this.prisma.user.findUnique({ where: { id: payload.sub } });
    if (!user) throw new UnauthorizedException();
    return { id: user.id, email: user.email, role: user.role };
  }
}
```

**Step 2: Guards et décorateur**

```typescript
// backend/src/auth/auth.guard.ts
import { Injectable } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

```typescript
// backend/src/auth/roles.decorator.ts
import { SetMetadata } from '@nestjs/common';
import { Role } from '@prisma/client';

export const ROLES_KEY = 'roles';
export const Roles = (...roles: Role[]) => SetMetadata(ROLES_KEY, roles);
```

```typescript
// backend/src/auth/roles.guard.ts
import { Injectable, CanActivate, ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { Role } from '@prisma/client';
import { ROLES_KEY } from './roles.decorator';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const requiredRoles = this.reflector.getAllAndOverride<Role[]>(ROLES_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (!requiredRoles) return true;
    const { user } = context.switchToHttp().getRequest();
    return requiredRoles.includes(user.role);
  }
}
```

**Step 3: Enregistrer dans AuthModule — mettre à jour providers**

Patch `auth.module.ts` : ajouter `JwtStrategy`, `RolesGuard` dans providers et `JwtAuthGuard` si besoin.

**Step 4: Vérifier la compilation**

```bash
cd backend && npx tsc --noEmit
```
Expected: silence

---

## Task 6: Test d'intégration — protéger une route

**Objective:** Vérifier que les guards fonctionnent avec un endpoint test.

**Files:**
- Create: `backend/src/auth/auth.controller.spec.ts` (ajouter tests guards)
- Modify: `backend/src/auth/auth.controller.ts` (ajouter route protégée)

**Step 1: Ajouter un endpoint /auth/me protégé**

```typescript
// Dans auth.controller.ts, ajouter :
import { Get, Req, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from './auth.guard';

@Get('me')
@UseGuards(JwtAuthGuard)
async me(@Req() req: any) {
  return req.user;
}
```

**Step 2: Test manuel rapide**

```bash
# Démarrer le serveur
cd backend && npm run start:dev &
sleep 5

# Register
curl -s -X POST http://localhost:3000/auth/register \
  -H 'Content-Type: application/json' \
  -d '{"email":"test@jogga.com","password":"Secret123","fullName":"Test User","phone":"+242060000000"}' | python3 -m json.tool

# Login
TOKEN=$(curl -s -X POST http://localhost:3000/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"test@jogga.com","password":"Secret123"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

# Accéder à /me
curl -s http://localhost:3000/auth/me -H "Authorization: Bearer $TOKEN" | python3 -m json.tool

# Tester sans token → 401
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/auth/me
```

Expected: 200 avec token, 401 sans token

---

## Task 7: ValidationPipe global

**Objective:** Activer la validation automatique des DTOs via ValidationPipe.

**Files:**
- Modify: `backend/src/main.ts`

```typescript
// backend/src/main.ts
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.useGlobalPipes(new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true }));
  await app.listen(3000);
}
bootstrap();
```

**Vérification :**

```bash
# Test — envoyer un RegisterDto avec email invalide → 400
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/auth/register \
  -H 'Content-Type: application/json' \
  -d '{"email":"notanemail","password":"123"}'
```
Expected: 400

---

## Résumé des fichiers

| Fichier | Action |
|---------|--------|
| `backend/src/prisma/prisma.service.ts` | CREATE |
| `backend/src/prisma/prisma.module.ts` | CREATE |
| `backend/src/auth/auth.module.ts` | CREATE |
| `backend/src/auth/auth.service.ts` | CREATE |
| `backend/src/auth/auth.controller.ts` | CREATE |
| `backend/src/auth/auth.guard.ts` | CREATE |
| `backend/src/auth/roles.guard.ts` | CREATE |
| `backend/src/auth/roles.decorator.ts` | CREATE |
| `backend/src/auth/jwt.strategy.ts` | CREATE |
| `backend/src/auth/dto/register.dto.ts` | CREATE |
| `backend/src/auth/dto/login.dto.ts` | CREATE |
| `backend/src/auth/auth.service.spec.ts` | CREATE |
| `backend/src/auth/auth.controller.spec.ts` | CREATE |
| `backend/src/auth/dto/register.dto.spec.ts` | CREATE |
| `backend/src/app.module.ts` | MODIFY |
| `backend/src/main.ts` | MODIFY |
| `backend/package.json` | MODIFY (deps) |

## Ordre d'exécution

1. Task 0 — Installer dépendances
2. Task 1 — Structure (PrismaModule, AuthModule)
3. Task 2 — DTOs validés
4. Task 3 — AuthService (register/login)
5. Task 4 — AuthController (endpoints)
6. Task 5 — JWT Strategy + Guards
7. Task 6 — Test intégration
8. Task 7 — ValidationPipe global

**Commit:** Après chaque task, `git add` + `git commit -m "feat(auth): <description>"`
